services:
  - type: web
    name: portfolio-rebalancer
    env: python
    plan: free
    buildCommand: ""
    startCommand: python server.py
    envVars:
      - key: DB_PATH
        value: /tmp/model_portfolio.db
